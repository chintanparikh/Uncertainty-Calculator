A super basic uncertainty calculator.

It's a very old project that I'll someday update to make the code cleaner and use actual equations instead of basic operations (and probably use JS instead of PHP)